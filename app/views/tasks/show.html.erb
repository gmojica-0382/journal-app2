<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>

<p>
  <strong>Note:</strong>
  <%= @task.notes %>
</p>

<p>
  <strong>Date:</strong>
  <%= @task.date %>
</p>
<p>
  <strong>Completed:</strong>
  <%= @task.completed %>
</p>

<%= link_to 'To All Tasks', tasks_url(@tasks) %> |
<%= link_to "#{@category.title} Category", category_url(@category) %> |
<%= link_to "Edit", edit_category_task_url(@category, @task) %> |
<%= link_to 'Delete', category_task_url(@category, @task), method: :delete, data: { confirm: "Are you sure?" } %>
